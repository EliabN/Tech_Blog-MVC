<!--TODO: WHEN I click on the button to add a new blog post
THEN I am prompted to enter both a title and contents for my blog post with my new blog post-->

<div class="text-center">
  <h2>{{title}}</h2>
  <p>{{content}}</p>
  <p>Post by {{user.name}} on {{format_date date_created}}</p>

  <!-- Loop over each comment in the comments array for this post -->
    {{#each comments as |comment| }}
        <div class="comment col-md-7">
          <p>{{comment.comment}}</p>
          <p>Comment by {{comment.user.name}} on {{format_date comment.date_created}}</p>
        </div>
      {{/each}}
  <p>
    <button class="btn btn-primary" id="add-comment" >Add Comment</button>
  </p>
</div>